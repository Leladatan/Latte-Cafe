import fs from '../../../Styles/index.module.scss'

export default function Fs() {
    return (
        <section className={fs.fs}>
            <div className={fs.container}>
                <div className={fs.fs__content}/>
            </div>
        </section>
    )
}